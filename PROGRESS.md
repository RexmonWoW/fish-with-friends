# Fish With Friends — Progress

This is the live status file. GDD.md is the plan; this is where things actually stand. Update it at the end of every Claude Code session — that's what lets a new session (or the planning chat) pick up in seconds instead of you re-explaining state.

Baseline below was reconstructed on 2026-08-27 from commit history and a quick read of the current files — it hasn't been kept current session-to-session yet, so treat "done" items as "looked done from the outside" and correct anything that's wrong.

Casting mechanic status was corrected on 2026-08-27 after actually reading the pipeline and running it headless — see Session Log for what was wrong and what got fixed.

## MVP Build Order status
1. [x] GodotSteam install + Steam connection — `steam_manager` autoload in place, Steam class verified. Export templates (Mac/Linux/Windows) not yet confirmed.
2. [x] GitHub setup across two machines, different OS
3. [x] Folder structure + scene tree — autoload/data/entities/scenes/systems/ui in place
4. [x] Fish data resource system — `FishData`, `CaughtFish`, `FishFactory` implemented. 5 placeholder species now exist (`sunfish`/`bluegill` Common, `largemouth_bass` Uncommon, `catfish` Rare, `golden_koi` Legendary) spanning a real size/value/rarity spread; still needs real species content from planning/content. Also fixed `SpawnPool._weighted_pick()`, which was a TODO stub always returning `pool[0]` — with only one species that was invisible, but it meant no amount of adding species would ever have produced variety until this was fixed too. Now a real weighted-random pick by `spawn_weight` (rarity-tiered weighting is a further refinement, still unowned).
5. [x] Casting mechanic — aim/charge/release/RPC/host water-validation/lure-arc pipeline confirmed working end-to-end via `tests/cast_smoke_test.gd` (headless, no display needed). Two real bugs found and fixed along the way: `EventBus` was missing the `cast_charge_started`/`cast_charge_updated` signals `rod.gd` and `cast_meter.gd` depended on, and `CastMeter` was never instantiated into the game scene. Playtest-found bug fixed: casts were computed along the camera's full 3D look direction, so aiming down at the water (natural while fishing) sent the endpoint's Y far below the surface and the water raycast missed it, rejecting the cast — fixed by flattening cast direction to horizontal-only (distance = horizontal reach, not 3D line-of-sight). Confirmed with `tests/cast_angle_smoke_test.gd` across 5 pitches × 3 powers. `Rod.current_map` (the caching mechanism the paragraph above used to describe) is **gone entirely now** — see the HIGH finding in Code Review Findings and its fix below; validation resolves the active map fresh from `NetworkManager` on every cast instead of caching a per-node reference.
6. [x] Reel minigame + QTE — full GDD mechanic in place (`ui/reel_minigame.gd`): hold-to-rise catch zone vs. a drifting fish icon (hold "cast," not "jump" — freed Space for QTE prompts), fill/drain meter, resolves back to `Rod.state = IDLE` either way. QTE layer: the fish periodically "runs," prompting a SPACE/arrow key within ~1.1s; miss it and the zone shrinks; miss 3 times and the line snaps (fish gone, no catch). Zero misses on a catch = perfect, threaded through to `FishFactory`'s existing 1.5x value bonus (that plumbing already existed, nothing had ever set the flag). Verified with `tests/reel_qte_smoke_test.gd`.
7. [x] Line tangling + tug of war — two different players' `Rod.LineCollider`s (an Area3D that existed as an unused stub — fixed shape sitting at the rod's origin, representing nothing) now dynamically track the real line each frame and detect real overlaps. Crossing while both rods are `WAITING_BITE` starts a host-authoritative tug of war (`systems/tangle_manager.gd`): mash to push a shared rope value toward your side, first to the edge wins (6s timeout breaks a stalemate). Winner's line is untouched; loser's line snaps (`Rod` resets itself to `IDLE` on `EventBus.tangle_resolved`, same self-resetting pattern as reel resolution) and their pending bite is canceled. UI (`ui/tangle_minigame.gd`) shown to both involved players. Verified with `tests/line_tangle_test.gd` (real Area3D-detected crossing, correct winner/loser, loser's bite actually canceled).
8. [~] Shared livewell — data layer real (`add_fish()`/`remove_fish()`/`is_full()`, now broadcast to every peer, not just the host), catches land in a slot with a placeholder capsule visual (sized by the fish's rolled size, tinted by species — static pose, swimming is next), and proximity interaction works: standing in the `InteractionZone` shows a popup (`LivewellDisplay`) with every slot's species/size/value/caught-by, and pressing 1-5 throws that fish overboard to free the slot (blocked from also firing the rod — see multiplayer/bugfix pass in Session Log). Still missing: real "negotiate when full" UX (a full livewell just discards silently with a warning), look/raycast targeting of an individual fish (current interaction is proximity-only, not GDD's "look + prompt").
9. [ ] Big fish event — not started
10. [ ] Capsize minigame — not started
11. [~] Walkable lobby — bare-bones version done, by request: `scenes/lobby/lobby.tscn` is a plain walkable room (4 spawn points, no shop/mirror/upgrades yet) players land in first; walking into its `StartTrigger` starts the round and loads the lake, host-authoritative so it happens identically for every peer. Real GDD lobby (shop counter, mirror, trophy case) is a future dispatch. Verified with `tests/lobby_start_round_test.gd`, which now also regression-covers the HIGH finding below (a second player's rod, validated host-side after round start, genuinely gets snapped to the real water height).
12. [ ] Round timer + day structure — not started (`run_state.gd` autoload is still the untouched default Godot template)
13. [ ] Quota system — not started (depends on #12)

## Infrastructure in progress (not on the numbered list, but underlies it)
- Networking (`network_manager.gd`) — P2P host/client via GodotSteam, lobby create + host implemented. **Confirmed working on a real 2-machine test**, after four rounds of genuine client-can't-play/can't-see bugs found and fixed in this session: (1) `MultiplayerSpawner.spawn_function` only registered host-side, so a joining client's spawner had nothing to run — every spawn silently failed on their machine. (2) Fixed that, but spawn bookkeeping (`spawned_players`, `spawned_local_player`) lived in a host-only function, so the client stayed stuck on the menu despite their player working underneath. (3) A signal-based fix for that didn't hold up on a real retest either — replaced with an explicit broadcast RPC + late-joiner catch-up, which finally did. (4) With both machines actually in the game, found both were rendering through player one's camera (nothing ever called `camera.current = true` for the local player specifically — invisible with a single player, obvious with two). **Confirmed by the user's own 2-machine test**: each player controls their own character, a catch on one machine shows up with identical stats on the other. This is the first real multiplayer confirmation all session — everything before this was solo/host-only verification. Note: this confirmation predates the lobby rework (pass 13) — see Code Review Findings.
- Player movement + equipment slots — RigidBody3D movement and equipment slot system in progress. Movement was near-frozen (plateaued ~0.2 m/s against a 5.0 m/s cap) because default RigidBody3D/StaticBody3D friction against the boat hull was eating almost all applied force — fixed with a low-friction `PhysicsMaterial` on both, plus retuned `move_force`/`linear_damp`. Verified with `tests/movement_smoke_test.gd`. Player visibility to other players fixed: `PlayerModel` was a completely empty node (no mesh — other players were invisible); now a capsule body tinted with a deterministic per-peer color. Also had to add `CameraRig`/`CameraPitch` rotation to `PlayerSync`'s replication so the held rod (now camera-attached, see item 5's session log) actually rotates to match a player's real aim direction for everyone watching, not just locally.
- Boat entity — just started. Currently a flat raft (hull only, no walls) per direction — the walls were also causing a hard full-stop on collision at speed.
- Lake map (`scenes/maps/lake.tscn`) — was a completely empty scene (no script, no children) as of session start. Now has a placeholder Environment/lighting, a large water plane in the `water_surface` group, the boat, livewell, `BiteEventManager`, and 4 player spawn points, enough to satisfy `Map._validate_contract()` and let casting be tested for real. Still placeholder box art — real geometry is Art & Polish's per the comment in `network_manager.gd`.

## Code Review Findings (planning-chat review, 2026-08-28)
_A full read of the actual `.gd`/`.tscn` files, not just this log — done because a session log can only report what its own tests happened to cover._

- **HIGH — FIXED (2026-08-28, same day as the review).** Root cause confirmed exactly as described. Fix taken was the review's "better" suggestion, not the minimal patch: removed `Rod.current_map` entirely rather than trying to keep a per-node cache correctly refreshed for every peer. `Rod._validate_and_land_cast` (already host-only code) now calls `NetworkManager.get_current_map()` fresh on every validation — nothing to go stale because nothing is cached. `tests/lobby_start_round_test.gd`'s old assertion tested the deleted mechanism, not the actual guarantee; replaced with a real regression test (spawn a second, non-local player after round start, validate their rod host-side, confirm the endpoint is genuinely snapped to real water height, not silently passed through unchanged). Full suite re-verified. Still not confirmed on a real second machine — that's next.
- **MEDIUM — reel outcome (win/loss/perfect) is entirely client-reported with no server-side plausibility check.** `Rod.request_reel_resolution(success, was_perfect)` validates *who* sent it, not whether the values are plausible — a modified client could always report a perfect catch. **Decision: accepted as-is.** Friends-only game per GDD, low stakes; not adding a server-side sanity check for now. Written down here so it's a deliberate trade-off, not an oversight. (Note: `TangleManager`'s mash-reporting has the same shape of trust — any peer can call `request_mash()` for themselves any time they're actually in a tangle, no rate limit beyond human input speed. Same trade-off, same reasoning, not calling it out as a separate open item.)
- **LOW — a couple of dead-code spots, not urgent**: `Fish.finalize_catch()` (`entities/fish/fish.gd`) is unused — the real catch path goes through `BiteEventManager` → `FishFactory` directly. `Rod`'s `RodSync` MultiplayerSynchronizer has an empty replication config (deliberately emptied in pass 9, never removed).
- **Resolved — the `allow_object_decoding` open question below**: checked the actual RPC surface. No `any_peer` RPC (client → host) accepts a Resource/Object argument; only host-authority broadcasts carry `FishData`/`CaughtFish`. Safe under the current threat model as reasoned below. One caveat the original note didn't have: this flag is *global* to `SceneMultiplayer`, not scoped to these specific RPCs — it needs re-review the moment *anything* untrusted enters the picture (public lobbies, mods, non-friend joins), not only "if new Resource RPCs get added."
- **Resolved — the map-load-race open question below**: turned out to be worse than originally described — see the HIGH finding above. Not a rare timing window; currently a deterministic, always-on gap for every client under the lobby flow added in pass 13.
- **Overall read**: solid for two days of solo/duo work — consistent style, genuinely host-authoritative architecture, and the session log's bug descriptions check out against the real code. The recurring pattern worth naming: every real multiplayer bug this session (including the HIGH one above) has come from code that only gets exercised by a second real peer, and the smoke-test suite is structurally solo/single-process so it can't catch that class of bug on its own. That's not a criticism of the tests — they've caught real regressions — just a reason to treat "passes headless" and "confirmed on two machines" as two different bars, especially right after a change like the lobby rework that touches per-peer state.

## Next Up
- **Re-confirm on a real 2-machine test** that client-side water validation AND line tangling actually work now (both fixed/built in code, neither proven against a real peer — by explicit choice, not oversight: user asked to keep building rather than stop and retest).
- Round timer + day structure (item 12), then quota system (item 13) — up next per direction.
- Real GDD lobby content (shop counter, mirror/cosmetics, trophy case of biggest fish caught) — the bare-bones lobby is just a room + start trigger right now, per explicit scope direction.
- No player-visible feedback on a rejected cast (`cast_failed` just silently hides `CastMeter`, no message). Now that the aim-angle bug is fixed a rejection should be rare (genuinely out of range/off the map), but worth a quick "can't cast here" flash so a future rejection doesn't read as "broken" again.
- Swimming livewell fish — currently a static placeholder capsule per slot. Next pass: replace with per-slot wander/bob motion.
- Real "negotiate when full" UX for the livewell — currently a full slot just silently discards the catch with a `push_warning`.
- Real fish species content from planning/content — 5 placeholders exist (`sunfish`, `bluegill`, `largemouth_bass`, `catfish`, `golden_koi`), still no Mythic-tier or real art/values.
- Rarity-tiered spawn weighting — `SpawnPool` does flat weighted-random by `spawn_weight` now, but not the tiered "roll a rarity first" scheme `spawn_weight`'s doc comment implies.
- QTE tunables (window 1.1s, 3 misses to snap, 0.05 shrink/miss, 2.2-4.5s between QTEs) are first-guess placeholder numbers, not playtested/balanced.
- Bobber/line/rod visuals are placeholder (a red sphere, a plain gray line, a tapered cylinder for the rod) — real fishing line/bobber/rod art is Art & Polish's, per the same convention as the rest of the placeholder geometry.
- Decide the reel-outcome trust question (Code Review Findings, MEDIUM) — accept as-is or add a server-side sanity check.

## Open Questions
_(design/scope questions that came up mid-build and need a call from the planning chat — log them here instead of guessing)_
- **Made a security-relevant call, flagging for visibility**: fixing the multiplayer gaps below required `FishData`/`CaughtFish` (Resources) to travel over RPC, which `SceneMultiplayer` refuses by default (`allow_object_decoding = false`) since decoding arbitrary Objects off the wire is a real attack surface on an untrusted network. Enabled it in `NetworkManager._allow_resource_rpcs()`, judged safe here because GDD scopes this game to Steam-friends-only P2P lobbies (not public matchmaking) and the resources involved are plain `@export` data with no side-effecting code. Worth a second look from planning if the trust model ever changes (public lobbies, matchmaking, etc.) — decomposing to primitive-field RPCs instead would be the safer-but-more-verbose alternative. **RESOLVED — see Code Review Findings above.**
- `Rod.current_map` is set once, in `EquipmentSlot.equip_rod()`, from whatever `NetworkManager.get_current_map()` returns at that moment. For the host this is safe (map loads before players spawn), but a joining client's map-load RPC and their own player spawn/equip aren't ordered against each other, so a client could theoretically equip before their map finishes loading (current_map stays null → permissive fallback, not a crash, just unvalidated water for a frame). Not yet tested against a real second machine. **RESOLVED (and worse than described) — see Code Review Findings above.**

## Session Log
_(optional but useful — one line per session: date, what got done)_
- 2026-08-27 — Set up CLAUDE.md + PROGRESS.md, reconstructed this baseline from commit history, replaced GDD.md's old 5-chat hierarchy with a 2-lane (planning chat / Claude Code) workflow.
- 2026-08-27 — Verified and finished the casting mechanic end-to-end. Fixed two real bugs (EventBus missing `cast_charge_started`/`cast_charge_updated` signals; `CastMeter` never instantiated). Wired `Rod.current_map` so water validation is live. Built a placeholder Lake map (water plane, boat, livewell, spawn points, BiteEventManager) since it was a completely empty stub blocking any real test. Added `tests/cast_smoke_test.gd(.tscn)` as a headless, no-display way to drive the full cast pipeline and confirmed it works (charge → release → RPC → host water validation → lure arc → WAITING_BITE) using the GodotSteam editor console binary on this machine.
- 2026-08-27 (same day, second pass) — Playtest feedback: movement was "very very slow" and casting only worked once then went dead. Fixed both. Movement: default RigidBody3D/StaticBody3D friction against the boat hull was eating almost all applied force (measured plateau ~0.2 m/s via `tests/movement_smoke_test.gd`); added low-friction PhysicsMaterial + retuned `move_force`/`linear_damp`, now reaches the 5.0 m/s cap in <0.5s. Boat simplified to a raft (walls removed) per direction. Stuck cast: `Rod.state` had no path from `WAITING_BITE` back to `IDLE`, so every cast after the first silently no-op'd — root-caused and fixed by building a first-slice reel minigame (`ui/reel_minigame.gd`) that resolves the state back to `IDLE`. Along the way fixed a pre-existing bug where spawned `Fish` nodes got `global_position` set before `add_child`, silently failing (fish always spawned at the origin). Added a placeholder `sunfish` species so the bite pipeline had something to roll. Verified with `tests/reel_smoke_test.gd`.
- 2026-08-27 (same day, third pass) — Wired a successful catch into the livewell: `Livewell.add_fish()`/`remove_fish()`/`is_full()` plus `BiteEventManager` building a real `CaughtFish` via the already-implemented `FishFactory` on catch. Closes the full core loop (cast → bite → reel → catch → livewell) end to end for the first time. `tests/reel_smoke_test.gd` now also checks the livewell slot fills with the right species/size/value.
- 2026-08-27 (same day, fourth pass) — Added a visible placeholder for each livewell catch: `Livewell._spawn_visual()` parents a small capsule (rarity-tinted) to the filled slot's `FishDisplay/SlotN` marker, removed again by `remove_fish()`. Static placement only — swimming fish is next. Confirmed with `tests/reel_smoke_test.gd`.
- 2026-08-27 (same day, fifth pass) — Livewell proximity popup + throw-overboard: `LivewellDisplay` shows all 5 slots' stats while the local player stands in the (previously unwired) `InteractionZone`, and pressing 1-5 throws that fish overboard via a new `Livewell.request_remove_fish()` RPC. Verified with `tests/livewell_interaction_smoke_test.gd`.
- 2026-08-27 (same day, sixth pass) — "I want a few fish, with different sizes/colors": added 4 more placeholder species (was just `sunfish`) and fixed `SpawnPool._weighted_pick()`, a TODO stub that always returned `pool[0]` — invisible with one species, but would've silently blocked variety no matter how many got added. Livewell capsules now scale by the fish's actual rolled size and tint by a deterministic per-species color instead of a fixed size/rarity-only tint. Verified with `tests/spawn_variety_smoke_test.gd` (500 rolls, all 5 species show up proportional to spawn_weight) and `tests/livewell_visual_variety_test.gd`.
- 2026-08-27 (same day, seventh pass) — Playtest feedback: "sometimes the casts dont go thru an then u cant re cast." Root-caused with `tests/cast_angle_smoke_test.gd`: cast endpoint used the camera's full 3D forward vector, so looking down at the water sent the endpoint's Y well below the surface and the validator's raycast missed it — reproduced 8/15 rejections across a pitch/power grid before the fix, 0/15 after. `Rod.state` itself wasn't actually getting stuck (confirmed it always resolved back to IDLE via `_cast_failed`); the "can't recast" feeling was every subsequent attempt failing the same way with no feedback explaining why.
- 2026-08-27 (same day, eighth pass) — Finished the reel QTE layer (rest of GDD item 6): fish-runs-triggers-a-prompt, miss-shrinks-the-zone, 3-misses-snaps-the-line, perfect-catch-bonus, all now real and verified (`tests/reel_qte_smoke_test.gd`). Moved reel rise-control from "jump" to "cast" to free Space for QTE prompts (safe: Rod's own state guards make casting a no-op while REELING). Added a persistent fishing line + bobber (`lure_animator.gd`) that stays out until the reel resolves instead of hiding right after the arc; caught along the way that the cast endpoint was never actually snapped to the real water height (bobber would've floated at eye level) and fixed `WaterValidator` to return the actual hit point. Player movement/jump now locks entirely while a cast is out (charging through reeling), not just at the moment of a successful cast, per request. Also caught a `MeshInstance3D.set_surface_override_material` bug (needs an existing surface; a fresh `ImmediateMesh` has none) via `material_override` instead.
- 2026-08-27 (same day, ninth pass) — Playtest feedback: accidental casts while at the livewell, and the new line/bobber "ten miles above the player's head" / "launches into the sky instead of forward." Root cause of the visual bug: `AttachPoint_Hand` in `player.tscn` was Y=1.2 relative to the player's origin, which is the CENTER of a 1.8m capsule, not the feet — the rod tip (and everything attached to it) was floating ~2.1m up. Fixed to Y=0.2, plus dropped the arc's apex fraction 0.3→0.12 since the inflated start height was compounding with an already-tall apex. Accidental casting fixed with `Rod._near_livewell`, gated off the already-local `EventBus.livewell_proximity_changed`. Also did the real multiplayer pass asked for: `bite_started`/`reel_finished` and livewell `add_fish`/`remove_fish` were all host-only local `.emit()`s that never reached other peers — converted to broadcast RPCs (needed enabling `allow_object_decoding`, see Open Questions). Cleaned up `Rod`'s `RodSync` replication config, which was redundantly/riskily re-syncing `.state` (already correct via the broadcast RPCs) and `.rotation` (never set by anything) from a default authority that was never assigned to the owning peer. All of this is re-verified against the existing headless suite (still solo/host-only by nature) but **not yet tested against a real second machine** — that's explicitly next.
- 2026-08-27 (same day, tenth pass) — Playtest feedback: QTE arrows don't accept WASD, and the line/bobber looked "attached to the camera," swinging weirdly when turning. Root cause of the swing: the held rod's attach point was under `BodyPivot`, which only soft-follows camera yaw with a lag by design (so physics bumps don't yank the view) — invisible before, obvious once there was a persistent line to reveal it. Moved the hand attach point to be a child of the camera instead (rigid, like a normal FPS held item; `AttachPoint_Back` stays body-relative on purpose, for the unused stowed-rod case). Also gave the rod an actual mesh (was a completely empty `RodModel` node) and moved `RodTip` to its far end instead of the grip. Verified geometry and camera-rigidity headlessly (`tests/rod_mesh_geometry_test.gd`). QTE prompts now accept WASD as well as arrows.
- 2026-08-27 (same day, eleventh pass) — Audit before the real second-machine test: "make sure they can see each other." Found `PlayerModel` was a completely empty node (no mesh -- other players were literally invisible), fixed with a per-peer-tinted capsule body. Also found a bug the previous pass introduced: moving the held rod to be camera-relative meant it needed `CameraRig`/`CameraPitch` rotation replicated too (only `BodyPivot`'s yaw was), since those nodes only update locally for the owning peer -- without this, every other player would've seen a given player's rod frozen facing whatever direction it spawned in, never matching their real aim. Verified with `tests/player_visibility_test.gd`. Still nothing here has touched a real second peer -- that's the actual next step, per the user's plan to test on their second machine next.
- 2026-08-27 (same day, twelfth pass) — **First real 2-machine test.** Host could see and play normally; the joining client could Steam-invite/join fine but got stuck on the menu ("waiting for lobby..."), couldn't control their player at all, and quitting correctly showed up as a disconnect on the host's side. Root cause: `MultiplayerSpawner.spawn_function` (the callback each peer needs locally registered so it can reconstruct a replicated spawn) was only ever wired up in host-only code paths (`_on_lobby_created`, and `_spawn_player_for_peer` which is itself always called from a host-guarded context). A joining client's own spawner never got it set, so every spawn replicated from the host -- including the client's own player -- silently failed to construct on their machine. Fixed by calling `_get_spawner()` in `NetworkManager._ready()`, which now runs for every peer before any lobby action; confirmed (not just assumed) `GameRoot` already exists at that point via a temporary debug print, so this isn't a boot-order race. **Still needs re-testing on the second machine** -- this fixes the specific "can't play at all" blocker, but nothing past that point (does the client actually see the host, does casting/livewell work from their side) has been verified against a real peer yet.
- 2026-08-28 (second 2-machine test) — Progress: the client's player now genuinely worked (host saw them get pushed around, host saw them cast), but the client's own screen stayed stuck on the menu ("Joined lobby! Waiting for game..."), seeing nothing. Root cause: the previous fix made the client's Player node construct correctly, but ALL the bookkeeping around a spawn (populating `NetworkManager.spawned_players`, emitting `spawned_local_player` which `MainMenu` listens for to hide itself) lived inline in `_spawn_player_for_peer()`, which is only ever called host-side -- the client's own spawn happens through a totally different path (the spawner's internal replication mechanism calling `spawn_function` directly) that never touches that bookkeeping at all. First fix attempt (route everything through `MultiplayerSpawner`'s `spawned` signal) actually broke solo/host spawning entirely -- confirmed empirically with debug prints that the signal does NOT fire for the peer that originates the `.spawn()` call, only for peers receiving it via replication -- caught immediately by the existing headless suite hanging instead of passing, before it went anywhere near a real test. Fixed properly with a split path: whoever calls `.spawn()` (always the host) registers directly from the return value; everyone else registers via the `spawned` signal. Also would have fixed a latent client-side `ReelMinigame` bug for free, since it looks the caster up through `spawned_players` too. **Still needs re-testing** -- this is round two of "the client can't actually play," not yet confirmed fixed against a real peer.
- 2026-08-28 (third 2-machine test) — Client STILL stuck on the menu after the "spawned" signal fix. That signal-based approach was never actually proven for the case that mattered (a receiving peer registering a replicated spawn) -- only that it doesn't fire for the originating peer -- and even if it had worked, it can't retroactively catch a late-joining client up on players who spawned before they connected (the host's own player, notably). Replaced the whole mechanism with what's actually been proven reliable all session: an explicit broadcast RPC (`_notify_player_registered`), plus an explicit catch-up loop in `_on_peer_connected` so a newly-joined peer learns about every already-spawned player, not just their own. Since the RPC and MultiplayerSpawner's own node replication travel separate channels with no guaranteed ordering, the registration handler polls for the node (up to ~2s) instead of assuming it's already there. Re-verified against the full headless suite. **Still not confirmed against a real peer** -- if this doesn't hold either, next step should be temporary debug prints on both machines to see exactly which step fails, not another blind fix.
- 2026-08-28 (fourth 2-machine test) — Both machines now actually in the game (menu fix confirmed working!), but both rendered through player one's camera. Root cause: nothing anywhere ever called `camera.current = true` for the local player specifically -- invisible with one player (Godot auto-activates the sole `Camera3D`), obvious with two. Fixed in `Player.setup_for_peer()`. Couldn't verify with the usual headless test this time since Steam itself wasn't running locally (`tests/camera_ownership_test.gd` written but unrun) -- flagged as unverified rather than claimed confirmed.
- 2026-08-28 (fifth 2-machine test) — **Confirmed working.** Each player controls their own character; a fish caught on one machine shows up with identical stats on the other. First genuine multiplayer confirmation all session -- everything before this point was solo/host-only verification, however thorough. The camera fix held.
- 2026-08-28 (thirteenth pass) — Two requested follow-ups now that multiplayer is confirmed real. (1) Real Steam display names: `peer_id` isn't a Steam ID (SteamMultiplayerPeer assigns its own, forcing the host to 1 per Godot's MultiplayerAPI contract) -- added `SteamManager.get_display_name_for_peer()`, which resolves the real SteamID64 via `get_steam_id_for_peer_id()` and looks up the name via `getFriendPersonaName()`/`getPersonaName()`. Verified end to end: real name resolves and reaches `CaughtFish.caught_by_player_name`. (2) Bare-bones walkable lobby (item 11): players now land in a plain room first, not the lake directly; walking into a `StartTrigger` starts the round for everyone. Caught a real bug along the way -- removing a `CollisionObject` synchronously from inside the trigger's own physics callback isn't allowed, fixed by deferring the scene-swap. This changed a foundational test assumption (used to spawn straight into the lake), so 6 existing tests needed updating to explicitly start the round first. Full 14-test suite re-verified clean.
- 2026-08-28 (planning-chat review) — Read every script and key scene fresh (not relying on this log) to sanity-check status and catch what solo/headless tests structurally can't. Found one HIGH-severity regression (client water validation, introduced by pass 13's lobby rework), one MEDIUM design call to make (reel-outcome trust), two LOW dead-code spots, and resolved both prior Open Questions. Full detail above under Code Review Findings.
- 2026-08-28 (fourteenth pass) — Fixed the HIGH finding same-day. Removed `Rod.current_map` entirely instead of patching the refresh logic — host-only validation now asks `NetworkManager.get_current_map()` fresh every cast, closing off the whole class of "did we remember to refresh the cache for this peer" bug rather than just this one instance of it. Rewrote `lobby_start_round_test.gd`'s assertion to actually prove the fix (second player's rod, validated host-side post-round-start, gets genuinely snapped to real water) instead of checking a field that no longer exists. Hit the same lambda-connect/disconnect flakiness this session already learned to avoid once (cast_angle_smoke_test) while writing that new assertion — same fix, persistent connections + a member variable instead of per-call connect/disconnect. Full 14-test suite clean.
- 2026-08-28 (fifteenth pass) — Line tangling + tug of war (item 7), by direction (explicitly skipping a 2-machine retest first, to keep building). `Rod.LineCollider` was an unused Area3D stub (fixed default shape at the rod's origin) -- `LureAnimator` now dynamically resizes/orients it every frame to actually span the real line, universally (not owner-gated), the same lesson just learned from the water-validation bug: host-authoritative detection needs the HOST's own local mirror of every rod to update correctly, not just each peer's own. New `TangleManager` (host-authoritative, alongside `BiteEventManager` in `lake.tscn`) starts a mash-to-pull-rope tug of war when two different players' lines overlap while both `WAITING_BITE`; loser's line snaps and their pending bite is canceled, winner is untouched. Verified with `tests/line_tangle_test.gd` -- real Area3D detection (two rods landed at the same point, guaranteeing their lines cross) plus correct win/loss resolution. Also formally decided the MEDIUM finding from the review (reel/tangle outcomes are client-reported, no server-side sanity check) -- accepted as a deliberate trade-off for a friends-only game, not adding validation now. Full 15-test suite clean.
